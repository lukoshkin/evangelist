# Open text files clicked in the terminal in Neovim (new kitty tab).
# This handles OSC 8 file:// hyperlinks (e.g., from Claude Code diffs).
protocol file
mime text/*
action launch --type=tab nvim ${FILE_PATH}
