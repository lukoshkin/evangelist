[Slice]
# Tell systemd-oomd to deprioritize anything inside system.slice when picking
# a kill target. With this set, kills are funneled into user.slice (chrome,
# slack, spotify, telegram, etc.) rather than NetworkManager / dbus / udev /
# any other system service.
ManagedOOMPreference=avoid
