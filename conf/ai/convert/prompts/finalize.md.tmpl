# Finalize the migrated config for @TOOL@

A deterministic converter has just generated @TOOL@ configuration from
the canonical Claude Code artifacts at `@CLAUDE_SRC@`.

Current locally observed `@TOOL@` version: `@CURRENT_TOOL_VERSION@`
Last recorded tested version: `@RECORDED_TESTED_VERSION@`
Version stamp file: `@STAMP_PATH@`

Your job — review and correct that generated output:

1. Compare each generated skill/command/instructions file against its
   source under `@CLAUDE_SRC@`. Fix anything that did not translate:
   frontmatter keys, file placement, broken references.
2. Confirm helper-script paths in the generated files resolve to real
   files (they should point into `@CLAUDE_SRC@/scripts/`).
3. Set up any non-converted, tool-native local config that the converter
   skips (for example MCP servers and other assistant-specific user
   settings). Follow any tool-specific guidance below when present.
4. After the migration is verified, update `@STAMP_PATH@` so it records
   the exact version string you successfully tested against. Preserve the
   `tool:` line, set `tested-version:` to the validated version, and set
   `status: verified`.
5. Report what you changed, stamped, and anything you could not resolve.

@TOOL_EXTRA_GUIDANCE@

Do not edit files under `@CLAUDE_SRC@` — that is the Claude Code source
of truth.
